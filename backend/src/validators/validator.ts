import { isCategory, isEmail, isName, isPassword, isUserName } from "@/validators/strings";

const valid = {
    // strings
    isEmail,
    isUserName,
    isName,
    isPassword,
    isCategory
};

export default valid;